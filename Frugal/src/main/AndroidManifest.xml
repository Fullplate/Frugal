<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fullplate.frugal" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".activities.StreamActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.StatisticsActivity"
            android:label="@string/action_statistics"
            android:screenOrientation="portrait"
            android:parentActivityName=".activities.StreamActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="fullplate.frugal.activities.StreamActivity" />
        </activity>

        <activity
            android:name=".activities.PreferencesActivity"
            android:label="@string/action_preferences"
            android:screenOrientation="portrait"
            android:parentActivityName=".activities.StreamActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="fullplate.frugal.activities.StreamActivity" />
        </activity>

    </application>

</manifest>
